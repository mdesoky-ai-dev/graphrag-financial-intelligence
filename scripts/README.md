# scripts/

Operational CLIs. Small, disposable.

Planned:
- `provision_neo4j.py` — create indexes and constraints on a fresh Aura instance
- `provision_pinecone.py` — create the index with correct dimensions
- `reset_graph.py` — nuke all nodes and relationships (local dev only)
- `export_graph.py` — dump the graph as JSON for inspection
